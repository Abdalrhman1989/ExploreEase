// src/__mocks__/firebase.js

export const auth = {
    // Mock necessary auth functions or properties if needed
    currentUser: null,
  };
  
  export const db = {
    // Mock necessary Firestore functions or properties if needed
  };
  